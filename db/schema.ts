import { pgEnum, pgTable, text, timestamp, uuid, jsonb } from "drizzle-orm/pg-core";

export const role = pgEnum("role", ["participant_male", "participant_female", "guardian", "admin_male", "admin_female", "mediator", "super_admin"]);
export const processStatus = pgEnum("process_status", ["draft", "awaiting_female_consent", "awaiting_guardian_consent", "active_taaruf", "istikharah", "nazhor", "khitbah", "married", "withdrawn", "closed"]);
export const users = pgTable("users", { id: uuid("id").defaultRandom().primaryKey(), email: text("email").notNull().unique(), role: role("role").notNull(), displayCode: text("display_code").notNull().unique(), createdAt: timestamp("created_at").defaultNow().notNull() });
export const taarufProcesses = pgTable("taaruf_processes", { id: uuid("id").defaultRandom().primaryKey(), maleParticipantId: uuid("male_participant_id").notNull(), femaleParticipantId: uuid("female_participant_id").notNull(), guardianId: uuid("guardian_id"), status: processStatus("status").default("awaiting_female_consent").notNull(), deadlineAt: timestamp("deadline_at"), createdAt: timestamp("created_at").defaultNow().notNull() });
export const consents = pgTable("consents", { id: uuid("id").defaultRandom().primaryKey(), processId: uuid("process_id").notNull(), actorId: uuid("actor_id").notNull(), kind: text("kind").notNull(), grantedAt: timestamp("granted_at"), policyVersion: text("policy_version").notNull(), metadata: jsonb("metadata") });
export const auditLogs = pgTable("audit_logs", { id: uuid("id").defaultRandom().primaryKey(), actorId: uuid("actor_id"), action: text("action").notNull(), targetType: text("target_type").notNull(), targetId: uuid("target_id"), metadata: jsonb("metadata"), createdAt: timestamp("created_at").defaultNow().notNull() });
